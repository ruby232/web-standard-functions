/**
 * Copy a string from other
 * @param target
 * @param source
 * @return string
 */

export const fromString = (target: string, source: string): string => {
  target = source;
  return target;
};
