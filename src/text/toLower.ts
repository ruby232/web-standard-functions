/**
 * Return the specified character expression in lowercase letters.
 * @param {string} value
 * @return string
 */

export const toLower = (target: string): string => {
  return target.toLowerCase();
};
