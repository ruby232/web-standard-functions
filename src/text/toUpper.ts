/**
 * Return the specified character expression in uppercase letters.
 * @param {string} value
 * @return string
 */

export const toUpper = (target: string): string => {
  return target.toUpperCase();
};
